# 2026-08-20

- flutter_web_01: 修复图片加载报错（AssetImage 找不到 lib/images/img1.png）。原因是图片文件名拼写错误（ing1.png → img1.png），已通过 ren 重命名修复。pubspec.yaml 中 assets 声明（- lib/images/）本身正确，无需改动。
- flutter_web_01: 用户再次贴出 AssetImage 报错末尾两行（Image provider/key），复查确认图片文件、pubspec、路径均正确。原因指向运行中的 flutter run 未重启导致 AssetManifest 未重新生成（web 端热重载 r 不更新资源清单）。解决：按 R 热重启或重新 flutter run -d chrome，必要时 flutter clean。
- flutter_web_01: 【最终根因】图片一直 404 显示不出的真正原因是 pubspec.yaml 中 `assets:` 缩进错误——写在了 `flutter:` 平级（顶层），而非 `flutter:` 子级，导致 Flutter 未识别任何资源，AssetManifest.bin 里只有 cupertino 字体。已修正为缩进 2 空格（`flutter: > assets: > - lib/images/`）。诊断方法：用 dev server 端口（netstat 查 LISTENING + flutter_tools.snapshot run 进程）直接 Invoke-WebRequest 请求 /assets/AssetManifest.bin 和图片 URL 验证 404/内容。已停掉旧 dev server（PID 2244）和 build 进程，需重新 flutter run -d chrome。
